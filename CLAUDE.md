# Cubound

작은 큐브가 칸을 하나씩 건너 **집(구멍)**을 찾아가는 미니멀 아이소메트릭 퍼즐 게임.

## 작업 원칙

**모든 작업에 가장 먼저 적용한다.** 속도보다 신중함 쪽으로 기울어 있으니, 오타 수정 같은 사소한 작업은 판단해서 가볍게 한다.

### 1. 코딩 전에 생각한다

**추측하지 않는다. 헷갈림을 숨기지 않는다. 트레이드오프를 드러낸다.**

- 가정은 구현 전에 명시한다. 확실하지 않으면 묻는다
- 요청이 여러 뜻으로 읽히면 하나를 몰래 고르지 말고 선택지를 보여준다
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다
- 불분명한 게 있으면 멈추고, 무엇이 헷갈리는지 말하고, 묻는다

### 2. 단순함이 먼저다

**문제를 푸는 최소한의 코드만 쓴다. 추측성 코드는 쓰지 않는다.**

- 요청받지 않은 기능은 추가하지 않는다
- 한 번만 쓰는 코드에 추상화를 만들지 않는다
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다
- 일어날 수 없는 상황에 대한 에러 처리를 넣지 않는다
- 200줄로 짰는데 50줄로 될 것 같으면 다시 쓴다

기준: "시니어 엔지니어가 보면 과하다고 할까?" 그렇다면 단순하게 줄인다.

### 3. 필요한 곳만 정확히 고친다

**꼭 필요한 부분만 건드린다. 내가 만든 흔적만 정리한다.**

- 주변 코드, 주석, 포맷을 "개선"하지 않는다
- 고장 나지 않은 것을 리팩터링하지 않는다
- 내 방식과 달라도 기존 스타일을 따른다
- 관련 없는 죽은 코드를 발견하면 지우지 말고 알려준다
- 내 변경 때문에 안 쓰이게 된 import, 변수, 함수는 지운다. 원래 있던 죽은 코드는 요청이 없으면 그대로 둔다

기준: 바뀐 모든 줄이 사용자의 요청으로 곧장 이어져야 한다.

### 4. 목표 기준으로 실행한다

**성공 기준을 정하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다.

| 요청            | 검증 가능한 목표                             |
| --------------- | -------------------------------------------- |
| "검증 추가해줘" | 잘못된 입력에 대한 테스트를 쓰고, 통과시킨다 |
| "버그 고쳐줘"   | 버그를 재현하는 테스트를 쓰고, 통과시킨다    |
| "리팩터링해줘"  | 변경 전과 후 모두 테스트가 통과한다          |

여러 단계 작업은 짧은 계획을 먼저 말한다.

```
1. [단계] → 확인: [검증 방법]
2. [단계] → 확인: [검증 방법]
3. [단계] → 확인: [검증 방법]
```

"동작하게 만들기" 같은 약한 기준은 계속 되묻게 만든다. 강한 기준이 있어야 스스로 반복하며 끝낼 수 있다.

**이 프로젝트에서:** 게임 규칙(`apps/web/src/game/`)과 풀이 검사기는 순수 함수라 테스트로 검증하기 쉽다. 규칙을 추가하거나 고칠 때는 해당 상황의 테스트부터 쓴다. 작업을 마치면 `pnpm build`, `pnpm lint`, `pnpm test`로 확인한다.

## 서비스 정체성

- 이름: **Cubound** = Cube + bound(~로 향하는). "어딘가로 향하는 큐브의 여행"
- 핵심 루프: 필드 관찰 → 상자, 스위치, 사다리로 길 만들기 → 구멍 도착 → 별점 → 다음 스테이지
- 성격: 개인 프로젝트. 웹 먼저, 이후 Capacitor로 안드로이드와 iOS 출시
- 제약: 운영비 **0원**. 서버, DB, 로그인 없음, 진행 상황은 기기에 저장. 나중에 붙일 계획은 `docs/ARCHITECTURE.md` "서버와 인증"

## 현재 상태

게임 규칙(이동, 높이, 상자, 스위치와 문, 사다리), 풀이 검사기, 스테이지 형식 검사, 화면(타이틀, 스테이지 선택, 게임, 클리어), 버전 있는 진행 저장, 연출(구르기, 연달아 이동, 구역 카메라, 가림 처리, 클리어 효과), 스텝 가이드까지 동작한다. 스테이지는 1스테이지(기본 이동)만 있다. 다음은 다국어 구조와 영어, 보스 이동 제한, 모바일 스와이프, 1월드 스테이지 2~10 제작.

## 프로젝트 구조

pnpm 워크스페이스 모노레포.

```
apps/
├── web/        # 게임 (Vite + React). 모든 게임 코드
└── server/     # 비어 있음. 나중에 NestJS
packages/       # 비어 있음. 나중에 web과 server가 함께 쓰는 코드
docs/           # 기획, 설계, 디자인 브리프
```

- 의존성은 각 앱의 `package.json`에 둔다. 루트 `package.json`에는 스크립트와 저장소 전체 도구(Prettier, Husky, lint-staged)만 둔다
- 여러 앱이 함께 쓰는 의존성 버전은 `pnpm-workspace.yaml`의 `catalog`에 두고 `"catalog:"`로 참조한다
- 공통 TypeScript 설정은 루트 `tsconfig.base.json`을 `extends`한다

## 문서 안내

| 위치                          | 내용                                                                                  | 읽히는 시점              |
| ----------------------------- | ------------------------------------------------------------------------------------- | ------------------------ |
| `CLAUDE.md` (이 파일)         | 작업 원칙, 정체성, 현황, 구조, 명령어, 스택                                           | 항상                     |
| `docs/PRD.md`                 | 게임 규칙, 오브젝트, 스테이지 구성, MVP 범위                                          | 규칙과 기능을 다룰 때    |
| `docs/ARCHITECTURE.md`        | 스택, 폴더 구조, 게임 모델, 렌더링, 애니메이션, 디자인 기준값, 저장, 서버와 인증 계획 | 구조를 다룰 때           |
| `docs/design/`                | Claude Design 프로토타입 (모양, 색, 치수 참고용)                                      | 화면을 구현할 때         |
| `docs/DESIGN_BRIEF.md`        | Claude Design 전달용 브리프                                                           | 화면을 다룰 때           |
| `.claude/rules/code/`         | 코드 작성 규칙                                                                        | 해당 파일을 다룰 때 자동 |
| `.claude/history.md`          | 만든 순서대로의 작업 이력                                                             | 필요할 때 직접 찾아봄    |
| `.claude/skills/update-docs/` | 작업 후 문서 갱신                                                                     | `/update-docs` 호출 시   |

규칙 파일과 적용 범위:

| 파일            | 적용 범위                        | 내용                                                                           |
| --------------- | -------------------------------- | ------------------------------------------------------------------------------ |
| `general.md`    | 항상                             | 버전 주의, 협업 방식, 주석, 명명, 위치, 포맷, 커밋                             |
| `components.md` | `apps/web/src/**/*.tsx`          | 컴포넌트 구조, 게임 화면, 모션, useEffect, 메모이제이션, 호버 색, 스크롤, 커서 |
| `game.md`       | `game/`, `stages/`               | 순수 로직, 규칙 추가, 스테이지 데이터와 형식 검사                              |
| `testing.md`    | `*.test.ts`, `e2e/`, 테스트 설정 | Vitest와 Playwright 범위, 화면 캡처 확인                                       |

작업을 마치면 `/update-docs`로 이력과 문서를 갱신한다. 기존 기능을 고치기 전엔 `.claude/history.md`에서 해당 항목을 먼저 찾아본다.

## 자주 쓰는 명령어

```bash
pnpm dev                          # 웹 개발 서버
pnpm build                        # 전체 빌드 (타입 검사 포함)
pnpm test                         # 전체 테스트
pnpm lint                         # 전체 린트
pnpm format                       # Prettier 정리
pnpm e2e                          # Playwright 스모크 테스트
pnpm shot                         # 화면 캡처 (apps/web/e2e/.screenshots)
pnpm --filter @cubound/web add <pkg>   # web에 패키지 추가
```

## 기술 스택

| 영역          | 선택                                                                                            |
| ------------- | ----------------------------------------------------------------------------------------------- |
| 패키지 매니저 | pnpm 9 워크스페이스 (Node.js 22.12 이상)                                                        |
| 빌드          | Vite 8                                                                                          |
| UI            | React 19 + TypeScript (strict)                                                                  |
| 게임 화면     | SVG (아이소메트릭)                                                                              |
| 애니메이션    | Motion                                                                                          |
| 상태          | Zustand                                                                                         |
| 스타일        | Tailwind CSS v4                                                                                 |
| 테스트        | Vitest (단위) + Playwright (스모크, 화면 캡처)                                                  |
| 코드 퀄리티   | oxlint + Prettier (import 정렬, Tailwind 클래스 정렬) + Husky와 lint-staged (커밋 전 자동 검사) |
| 배포          | Vercel                                                                                          |
